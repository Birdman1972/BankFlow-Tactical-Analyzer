# v0.3.9 Release Notes

**Release Date**: 2026-01-29
**Codename**: High-Tech Lab

## ✨ New Features

- **Cyberpunk Light Theme**: A cleaner, high-contrast interface designed for prolonged usage. Inspired by "High-Tech Lab" aesthetics (Clinical White + Neon Accents).
- **Theme Toggle**: Redesigned as a modern "Pill Switch" with sliding animations, located in the header.
- **Dynamic Theming**: Real-time CSS variable switching without page reload.
- **Fail-Safe File Validation**:
  - **Strict Header Check**: Automatically rejects files missing mandatory columns (`Transaction Date`, `Account`, etc.).
  - **Visual Feedback**: DropZone turns red and lists exact missing fields on error.
  - **Safety Lock**: Analysis buttons are disabled until valid files are loaded.

## 🐛 Bug Fixes

- **Vercel Runtime Crash**: Fixed `null is not an object` error caused by unsafe `<html ...>` tag injection in Svelte templates. Now uses safe reactive DOM manipulation.
- **WASM Export Stability**: Confirmed fix for `SystemTime` panic in browser environments by enabling `rust_xlsxwriter/wasm` feature.

## 📦 Build & Packaging

- **Windows Support**: Validated core logic on non-Windows endpoints; CI/CD pipeline enabled for `.msi` generation.
- **macOS Support**: Optimized `.dmg` bundling.
